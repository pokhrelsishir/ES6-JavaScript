const first = 'Bob';
const last = 'Builder';

const full = 'Bob\nBuilder';


const fullWithTick = `Bob
the freaking builder. Notice this will take care of spaces/whitespaces automatically.`;


console.log(full);
console.log(fullWithTick);